@AGENTS.md

## Claude Code

Prefer LSP operations (go-to-definition, find references, rename) over grep for
symbol navigation in Rust code.

<!-- Maintainer note: subsystem rules live in nested AGENTS.md files
     (crates/*/AGENTS.md, ui/AGENTS.md), each with a one-line CLAUDE.md redirect.
     Keep root AGENTS.md repo-wide-only; never duplicate a rule at two levels. -->
